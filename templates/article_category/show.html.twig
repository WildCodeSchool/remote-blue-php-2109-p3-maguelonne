{% extends 'base.html.twig' %}

{% block title %}ArticleCategory{% endblock %}

{% block body %}
    <h1>Catégory d'article</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ article_category.name }}</td>
            </tr>
            {# <tr>
                <th>Slug</th>
                <td>{{ article_category.slug }}</td>
            </tr> #}
        </tbody>
    </table>

    <a href="{{ path('article_category_index') }}">back to list</a>

    <a href="{{ path('article_category_edit', {'id': article_category.id}) }}">edit</a>

    {{ include('article_category/_delete_form.html.twig') }}
{% endblock %}
