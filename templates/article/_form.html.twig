
{{ form_start(form) }}

    <div class="form-group d-flex flex-column w-25 mb-3">
    {{ form_row(form.title) }}
    {{ form_row(form.body) }}
    {{ form_row(form.poster) }}
    {{ form_row(form.createdAt) }}
    {{ form_row(form.duration) }}
    {{ form_row(form.alt) }}
    </div>

    <button class="btn">{{ button_label|default('Publier') }}</button>
{{ form_end(form) }}
